console.log('🚀 Starting test: TestCase_1771155530530');

  // Step 1: Navigate to page
  console.log('📋 Step 1: Navigate to https://automationexercise.com/');
  await page.goto('https://automationexercise.com/', { waitUntil: 'domcontentloaded', timeout: 30000 });
  console.log('✅ Page loaded');

  // Step 2: click 'Signup / Login'
  console.log('📋 Step 2: click \'Signup / Login\'');
  const btn_signup_log_0 = page.locator('button, a, [role="button"]').filter({ hasText: 'Signup / Login' }).first();
  await btn_signup_log_0.waitFor({ state: 'visible', timeout: 10000 });
  await btn_signup_log_0.click();
  console.log('✅ Signup / Login clicked');
  await page.waitForTimeout(1000);

  // Step 3: verify 'Login to your account'
  console.log('📋 Step 3: verify \'Login to your account\'');
  await expect(page.locator('body')).toContainText('Login to your account', { timeout: 10000 });
  console.log('✅ Verified: Login to your account');

  // Step 4: enter email
  console.log('📋 Step 4: enter email');
  const emailField_0 = page.locator('#email, [name="email"], [type="email"], [data-test="email"]').first();
  await emailField_0.waitFor({ state: 'visible', timeout: 10000 });
  await emailField_0.fill('temp8268@gmail.com');
  console.log('✅ email filled with: temp8268@gmail.com');

  // Step 5: enter password
  console.log('📋 Step 5: enter password');
  const passwordField_1 = page.locator('#password, [name="password"], [data-test="password"], input[type="password"]').first();
  await passwordField_1.waitFor({ state: 'visible', timeout: 10000 });
  await passwordField_1.fill('temp8268');
  console.log('✅ password filled with: temp8268');

  // Step 6: click 'login'
  console.log('📋 Step 6: click \'login\'');
  const loginBtn_1 = page.locator('button:has-text("Login"), button:has-text("Sign in"), #login-button, [data-test="login-button"]').first();
  await loginBtn_1.waitFor({ state: 'visible', timeout: 10000 });
  await loginBtn_1.click();
  console.log('✅ Login button clicked');
  await page.waitForTimeout(2000);

  // Step 7: click LogOut
  console.log('📋 Step 7: click LogOut');
  const btn_logout_2 = page.locator('button, a, [role="button"]').filter({ hasText: 'LogOut' }).first();
  await btn_logout_2.waitFor({ state: 'visible', timeout: 10000 });
  await btn_logout_2.click();
  console.log('✅ LogOut clicked');
  await page.waitForTimeout(1000);

  // Take final screenshot
  await page.screenshot({ path: 'test-results/TestCase_1771155530530-final.png', fullPage: true });
  console.log('✅ Test completed successfully');